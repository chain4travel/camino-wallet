name: build-docker-image-aws

on:
  pull_request:
    branches:
      - c4t
      - dev
      - suite
      - suite-c4t
  push:
    branches:
      - c4t
      - dev
      - suite
      - suite-c4t

env:
  node_version: 16
  module_docker_image: 'public.ecr.aws/n0m7j6i5/camino-suite-wallet'
  aws_region: 'eu-central-1'

jobs:
  build_docker:
    name: Build Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Prepare Image Tag
        id: setDefaults
        run: |
          DOCKER_IMAGE_NAME="278308921038.dkr.ecr.eu-central-1.amazonaws.com/chain4travel-prod-ecr-camino-suite"
          if [[ "${{ github.ref }}" == "refs/heads/c4t" ]]; then
              echo "docker_image=$DOCKER_IMAGE_NAME:stage" >> $GITHUB_OUTPUT
              echo "build_env=stage" >> $GITHUB_OUTPUT
              echo "service_name=suite-stage" >> $GITHUB_OUTPUT
              echo "task_definition=suite-stage-task" >> $GITHUB_OUTPUT
              echo "tag=c4t" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == "refs/heads/suite-c4t" ]]; then
              echo "docker_image=$DOCKER_IMAGE_NAME:stage" >> $GITHUB_OUTPUT
              echo "build_env=stage" >> $GITHUB_OUTPUT
              echo "service_name=suite-stage" >> $GITHUB_OUTPUT
              echo "task_definition=suite-stage-task" >> $GITHUB_OUTPUT
              echo "tag=suite-c4t" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == "refs/heads/dev" ]]; then
              echo "docker_image=$DOCKER_IMAGE_NAME:dev" >> $GITHUB_OUTPUT
              echo "build_env=dev" >> $GITHUB_OUTPUT
              echo "service_name=suite-dev" >> $GITHUB_OUTPUT
              echo "task_definition=suite-dev-task" >> $GITHUB_OUTPUT
              echo "tag=dev" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == "refs/heads/suite" ]]; then
              echo "docker_image=$DOCKER_IMAGE_NAME:dev" >> $GITHUB_OUTPUT
              echo "build_env=dev" >> $GITHUB_OUTPUT 
              echo "service_name=suite-dev" >> $GITHUB_OUTPUT
              echo "task_definition=suite-dev-task" >> $GITHUB_OUTPUT
              echo "tag=suite" >> $GITHUB_OUTPUT
          else
              echo "docker_image=$DOCKER_IMAGE_NAME:temp" >> $GITHUB_OUTPUT
              echo "build_env=dev" >> $GITHUB_OUTPUT  
              echo "service_name=suite-temp" >> $GITHUB_OUTPUT
              echo "task_definition=suite-temp-task" >> $GITHUB_OUTPUT
              echo "tag=temp" >> $GITHUB_OUTPUT 
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.aws_region }}

      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region ${{ env.aws_region }} | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.aws_region }}.amazonaws.com

      - name: create module docker image
        run: |
          docker build . -t ${{ env.module_docker_image }}:${{ steps.setDefaults.outputs.tag }} --build-arg BUILD_ENV=build:${{ steps.setDefaults.outputs.build_env }}

      - name: push module docker image
        if: github.event_name == 'push'
        run: |
          docker push ${{ env.module_docker_image }}:${{ steps.setDefaults.outputs.tag }}

      - name: create suite docker image
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            docker build . -f Dockerfile_SUITE_AWS -t ${{ steps.setDefaults.outputs.docker_image }} --build-arg BUILD_ENV=build:${{ steps.setDefaults.outputs.build_env }} --build-arg SUITE_BRANCH=${{ github.base_ref }} --build-arg EXPLORER_BRANCH=${{ github.base_ref }} --build-arg VOTING_BRANCH=${{ github.base_ref }}
          else
            docker build . -f Dockerfile_SUITE_AWS -t ${{ steps.setDefaults.outputs.docker_image }} --build-arg BUILD_ENV=build:${{ steps.setDefaults.outputs.build_env }} --build-arg SUITE_BRANCH=${{ github.ref_name }} --build-arg EXPLORER_BRANCH=${{ github.ref_name }} --build-arg VOTING_BRANCH=${{ github.ref_name }}
          fi

      - name: push and deploy docker
        if: ${{ (github.event_name == 'push') && (github.ref  == 'refs/heads/suite') }}
        run: |
          docker push ${{ steps.setDefaults.outputs.docker_image }}          
          aws ecs update-service \
            --cluster chain4travel-prod \
            --service ${{ steps.setDefaults.outputs.service_name }} \
            --task-definition ${{ steps.setDefaults.outputs.task_definition }} \
            --force-new-deployment \
            --region ${{ env.aws_region }}

      - name: checkout camino-suite
        if: ${{ (github.event_name == 'push') && (steps.setDefaults.outputs.build_env  == 'stage') }}
        uses: actions/checkout@v3
        with:
          submodules: recursive
          fetch-depth: 0
          repository: 'chain4travel/camino-suite'
          ref: 'refs/heads/suite-c4t'
          path: './camino-suite'
          token: '${{ secrets.GIT_TAGS_TOKEN }}'

      - name: Create suite tag
        id: createTag
        working-directory: ./camino-suite
        if: ${{ (github.event_name == 'push') && (steps.setDefaults.outputs.build_env  == 'stage') }}
        run: |
          echo "create_tag=false" >> $GITHUB_OUTPUT
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          CURRENT_TAG=$(git describe --abbrev=0 --tags)
          if [[ $CURRENT_TAG == *"-rc"* ]]; then
            TAG=$(echo "$CURRENT_TAG"|awk -F'-rc' -v OFS='-rc' '{$2=sprintf("%1d",++$2)}7')
            git tag -a $TAG -m "create tag $TAG for staging"
            git push --tags
            echo "create_tag=true" >> $GITHUB_OUTPUT
            echo "suite_tag=$TAG" >> $GITHUB_OUTPUT
          fi

      - name: Create tag
        if: ${{ (github.event_name == 'push') && (steps.setDefaults.outputs.build_env  == 'stage') && (steps.createTag.outputs.create_tag == 'true') }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git tag ${{ steps.createTag.outputs.suite_tag }}
          git push --tags
